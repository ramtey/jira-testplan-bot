# --- Jira ---
JIRA_BASE_URL=https://your-domain.atlassian.net
JIRA_EMAIL=you@company.com
JIRA_API_TOKEN=YOUR_JIRA_API_TOKEN

# --- LLM Configuration ---
# Provider: "ollama" (local, free) or "claude" (Anthropic API, paid)
LLM_PROVIDER=ollama

# Model name (for Ollama: llama3.1, qwen2.5, etc.; for Claude: claude-3-5-sonnet-20241022)
LLM_MODEL=llama3.1

# Ollama server URL (only needed if using Ollama)
OLLAMA_BASE_URL=http://localhost:11434

# Anthropic API key (only needed if using Claude)
# Get this from your company's Anthropic account
ANTHROPIC_API_KEY=

# --- App ---
APP_ENV=local
