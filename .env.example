# --- Jira ---
JIRA_BASE_URL=https://your-domain.atlassian.net
JIRA_EMAIL=you@company.com
JIRA_API_TOKEN=YOUR_JIRA_API_TOKEN

# --- LLM Configuration ---
# Provider: "claude" (Anthropic API, recommended) or "ollama" (local, free)
LLM_PROVIDER=claude

# Model name (for Claude: claude-3-5-sonnet-20241022; for Ollama: llama3.1, qwen2.5, etc.)
LLM_MODEL=claude-3-5-sonnet-20241022

# Anthropic API key (required for Claude)
# Get your API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=

# Ollama server URL (only needed if using Ollama)
OLLAMA_BASE_URL=http://localhost:11434

# --- GitHub (Optional - Phase 3a) ---
# GitHub Personal Access Token for fetching PR details and code diffs
# Required scopes: repo (or public_repo for public repos only)
# Get your token from https://github.com/settings/tokens
GITHUB_TOKEN=

# --- App ---
APP_ENV=local
